VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
# Public app URL used for auth redirects (invite/reset links) — must be a
# real, publicly reachable domain, not localhost, or invited members on
# other devices can't open the link. Update when moving to a new domain.
VITE_APP_URL=https://dolphintennis.com
